# v0.67.1 生产反馈闭环 × macOS 签名与自动更新（Windows/Linux 预留）

> 创建时间：2026-08-23
> 最后更新：2026-08-24
> 当前状态：🚧 P1/P2 本地修复、测试与独立终审闭环；核心实现/发布链收口已 commit `377c54bb`、`8dac2864`、`f00d62c2`。用户于 2026-08-24 明确授权正式发布 macOS-only `v0.67.2`；Release Notes、package/lock 版本已由 `4d1a7e7e` 准备，准确发版工作树通过完整 5308 pass / 0 fail / 1 skip、Next production 136 routes 与 Electron bundle。下一步 push `main` 与不可变 tag `v0.67.2`，由 tag CI 收正式 Developer ID/notary/asset graph 证据；未修改 Sentry 设置或真实用户数据库。用户明确不做 clean-machine/RC-A → RC-B 真实验收，因此即使 CI 发布成功也只能如实报告 Shipped + 该已接受风险，不能把未跑项写成 `Smoke passed` / `Release ready`
> 关联账本：[B-031 / #246](issue-tracker.md)、[模型目录与推理强度统一适配](model-capability-reasoning-refresh.md)、[Sentry 遥测可信度](sentry-telemetry-reliability.md)、[macOS Safe Storage 签名修复](macos-safe-storage-signing-remediation-2026-08-12.md)

## 状态

| Phase | 内容 | 状态 | 完成门禁 |
|---|---|---|---|
| Phase 0 | 三条信号只读取证、复现矩阵与范围冻结 | 🟡 本地取证完成；GitHub Secret 名称/存在性已审计，Sentry production 与用户诊断待外部 | 未确认项保持 hypothesis |
| Phase 1 | 重开 B-031：GLM-5.3 真实旧库升级路径 | 🟡 Code + gen-0/后续完整三行历史 fixture + 五态合同完成；真实用户 smoke 待跑 | 受影响行形 fixture + 四个消费面一致 + 真实升级 smoke |
| Phase 2 | Sentry P1：隐私、SQLite 启动损坏、Electron ABI | 🟡 Code + tests + 本机 packaged Main→utility ABI 通过；production ingest/五架构待 CI | 真实 event/envelope、恢复路径、真实 utilityProcess package gate |
| Phase 3 | Sentry P2：stream、Provider 辅助任务、Windows 生命周期/工具错误 | 🟡 stream/EPERM 已收口；token-gated cohort 与其余分类待证据 | expected failure 0 Issue；产品故障 exactly-once；用户有可行动反馈 |
| Phase 4 | 发布身份：macOS notarization/stapling；Windows/Linux 延后 | 🟡 Apple Secret 已配置但未经过 CI；Windows/Linux 不进入 Release | 当前可自动安装的 Mac 先有 fail-closed 身份门禁 |
| Phase 5 | 原子更新产物与 stable/preview channel | 🟡 Mac-only workflow/verifier、单 universal ZIP、混入禁入、最小写权限与 draft→公开已实现；终审定向/全量/双构建通过，正式 CI 待跑 | Mac 安装包、metadata、blockmap 同构建同版本发布，preview 不污染 stable |
| Phase 6 | Electron Main updater、窄 IPC 与更新 UX | ✅ 本地实现与合同测试完成；非 Mac 缺资产提示与 busy check 反馈已闭环 | 后台检查/下载，用户确认重启；错误可恢复，不强杀活动任务 |
| Phase 7 | 跨版本真实升级、preview → stable 晋级与恢复演练 | 🚧 用户已授权发布 `v0.67.2`；真实升级验收按已接受风险跳过，正式 tag CI 待跑 | 不冒充 Smoke/Release ready；仅在 CI 完成并公开完整 Release 后报告 Shipped |

## 2026-08-23 Plan review remediation

| Finding | 裁决 | 计划修订 |
|---|---|---|
| P1：0.67.1 不具备 native updater，无法自动更新到首个 RC | 接受 | 矩阵拆为 `0.67.1 → RC-A` 手动 bootstrap 与 `RC-A → RC-B` 真实自动更新，macOS/Windows/Linux 同理 |
| P1：`10% → 50% → 100%` 未定义且与 metadata 不手写冲突 | 接受 | 本轮不使用 `stagingPercentage`；采用 internal preview 验收后一次晋级 stable，全量发布后出错只发更高 patch |
| P1：metadata 在三处 artifact upload 和 release glob 双层丢弃 | 接受 | Phase 5 明确要求同时修改 macOS/Windows/Linux `upload-artifact.path` 与 release job asset glob，并加四点 source-contract 门禁 |
| P2：stable-id / upstream-id 阻断机制描述不准 | 接受 | stable-id 非 catalog-managed 行只导致该 slot 跳过更新；仅 stable id 不存在且 upstream 被占时才阻断插入，classifier/fixture 分轴覆盖 |
| P2：关闭 native dump 与 ST-10 冲突 | 接受 | 若选择关闭/独立 opt-in，必须同步修订 `SentryTelemetry.md` ST-10、检查表、测试与决策日志 |
| P2：stream 与 EPERM 修复面不完整 | 接受 | 同时覆盖 skills marketplace install/remove 与 CLI tool install；EPERM/EACCES 落点为 `error-classifier.ts`，不重复改已收敛的 bash tool |
| P2：updater guardrail、旧类型合同与依赖 pin 缺失 | 接受 | Phase 6 新建 `Updater.md`，先裁决并对齐 Main/preload/types/hooks，启用前把 `electron-updater` 从 caret 改为 exact pin |
| P2：Phase 模板、fixture 历史与 release docs | 接受 | Phase 0–7 都补用户结果/明确不做；校准 fixture 表述；Phase 5 同步 `docs/rules/release.md` 与 Release guardrail |

## 2026-08-23 Implementation review remediation

| Finding | 裁决 | Fix / Verify / Guardrail |
|---|---|---|
| P1：DMG 在 builder 写 metadata 后 staple，导致 `latest-mac.yml` sha512 必然失效 | 接受 | `dmg.writeUpdateInfo=false`，mac updater graph 改为 ZIP-only；verifier 拒绝 metadata 中的 DMG。真实 builder 形状探针确认仅有 ZIP entry/ZIP blockmap、无 DMG blockmap；Release 规则同步 |
| P1：GLM 旧指纹只覆盖部分 sonnet 历史，haiku 与最老 catalog 行进入 conflict 死路 | 接受 | 从已发布 git 历史补齐真实 sonnet→`sonnet` 的 gen-0 `GLM-4.7`、`GLM-5-Turbo`/`GLM-5.2` 与 haiku→`haiku` 的 `GLM-4.5-Air` 指纹，删除无历史来源的 sonnet→`glm-5-turbo` 猜测；gen-0/后续完整旧目录 fixture 验证 sonnet/haiku 原位迁移、opus 保留 |
| P1：macOS `quitAndInstall` 被 close-to-hide 吞掉 | 接受 | Main 监听 Electron native `before-quit-for-update`，先抬 `isQuitting`/quitting phase 再让窗口关闭；Updater guardrail 与 source-contract test 锁定 |
| P1：migration/runtime recovery 错误被 health catch-all 伪装成 DB 损坏 | 接受 | startup code 拆为 corrupt/busy/migration/runtime-recovery；只有真实 integrity failure 可展示破坏性恢复，未知 health failure 不再带 DB marker；UI 文案不再虚构“已备份” |
| P1：start-fresh relaunch 会重新复制 legacy DB | 接受 | 完整备份后 fsync 持久化 fresh-start intent；下次 bootstrap 在 legacy migration 前消费 marker 并清除残留 DB/WAL/SHM，成功打开空库后才清 marker；覆盖 legacy resurrection/损坏循环反例 |
| P2：bare EPERM/EACCES 也被全局降噪 | 接受 | 只有 spawn/exec 上下文的 permission 文本归 user-action；仅含 error code 的普通 fs 产品故障保持 UNKNOWN/reportable |
| P2：`identity_conflict` 过载且 UI 不消费冲突详情 | 接受 | 当前 canonical SKU 存在时保持 current，额外旧行不拖死整个候选；真正 CAS 冲突由服务端重读确认并返回 409，Dialog 展示冲突模型与“检查模型”动作 |
| P2：Windows installer 未进入 attestation glob | 接受 | stable/preview attestation 同时覆盖 `CodePilot-*` 与 `CodePilot.Setup.*` |
| P2：第四条 media plan stream 同族反模式漏网 | 接受 | `media/jobs/plan` 也改为 single-owner writer + request/timeout cancellation，和 marketplace/CLI route 共用合同 |
| P2：瞬态 DB lock 进程级永久 blocked | 接受 | busy/locked 做有界 3 次重试，耗尽后进入可重试状态且不生成损坏备份；不缓存为永久 bootstrap failure |
| P2：preview updater 是否 no-op | 接受有边界的偏离 | preview 保持 official internal RC 可更新，才能完成 RC-A→RC-B；stable/preview 均须官方 workflow provenance bit，普通 local/fork 默认 no-op，guardrail 如实写边界 |
| P2：乐观 GLM 状态、`js-yaml` 隐式依赖、preview tag 污染、遥测标题与 DB test 注释 | 接受 | mutation 后重拉服务端状态；`js-yaml@4.1.1` 直接 exact dependency；stable 明确只接收 `vX.Y.Z`；标题改为匿名 JS 错误/版本健康；测试注释按真实因果修订 |

## 2026-08-24 Fix-round review remediation

| Finding | 裁决 | Fix / Verify / Guardrail |
|---|---|---|
| P1：gen-0 sonnet `GLM-4.7` 指纹缺失，计划却误标完整 | 接受；先承认状态失真再修 | git 历史确认 2026-03-09～03-28 的完整 identity 为 `sonnet/sonnet/GLM-4.7/{}`；补 fingerprint 与三行 gen-0 fixture。remediation、清单、guardrail 和决策日志同步改为包含该世代 |
| P2：database retry 在 `serverPort=null` 时静默 no-op | 接受 | Main 新增无端口初始重试分支，重新执行稳定端口 utility startup；IPC 返回真实 accepted，失败继续回到正确 DB/generic recovery state。source-contract test 锁住不得恢复 `!serverPort` 早退 |
| P2：migration/runtime 根因被低基数错误完全覆盖 | 接受 | wrap 前生成有界、路径/凭据脱敏 diagnostic；Main 只提取该 marker 到本地支持日志与“复制诊断”。原始 stack/路径不进入恢复页/Sentry |
| P2：`identity_conflict` 无行为断言 | 接受 | 构造 stable-id 与 current-id 均被非 canonical wire 占用的真实歧义，断言 fail closed + 精确 `conflictModelIds` |
| P3：各域 13 条非阻塞硬化项 | 先登记 #87；用户随后明确要求本轮全部修复；首次 13/13 声称被复审否定 | 先回退关闭状态，再补 ⑪ generation envelope/内容去重、⑫ 四 route 接线 + safe-stream 指引调和、⑬ zero-byte 真实因果，并吸收 updater/GLM/DB 零散漏项。#87 现按真实闭环文本解决；跨入口原子 updater fence 独立登记 active #88，不把二次 snapshot 写成原子修复 |
| P2：updater chat 活跃态漏掉 `streaming` | 接受 | `hasActiveSessionWork` 的 live-owner 交集补齐 `running` / `streaming` / `waiting_permission`；行为测试钉住正在流式输出且 lease 有效时必须阻断安装，Updater guardrail 同步列出完整枚举 |
| P3：第二次收口复审的恢复/更新 UX 与性能尾项 | 登记，不冒充本轮已修 | fresh-start conflict 打开备份、busy 快速失败取舍、历史备份全量重哈希、慢 handoff UI 闪烁、下载期重复检查反馈统一登记 active tech-debt #89，带重启条件与关闭标准 |
| P2：Mac-only Release 仍让 Windows/Linux 显示可下载的新版本 | 接受 | `/api/app/updates` 增加由真实资产选择结果驱动的 `platformAssetMissing`；不再把 Release 页塞进 `downloadUrl`。Dialog/About/Overview 用 en/zh 明说该版本无当前平台安装包，CTA 降级为“查看发布详情” |
| P3：Mac-only verifier/权限/发布可见性仍可收紧 | 接受并直接闭环 | stable/preview 对称拒绝 Windows/Linux 资产；Mac metadata 恰好一条 universal ZIP；workflow 顶层 read-only、写权限只给发布 job；完整 graph 先传 resumable draft，再一次公开，避免半 feed |
| P3：Windows 手工 job signer 前置与 updater 两条旧观察 | 部分新信息，部分已有记录 | 文档明确 Windows 手工 job 缺三项 signer 配置会 fail closed；下载期检查按钮本轮补反馈并从 #89 移出。15 秒极慢 handoff 闪烁原已登记 #89，缺真实复现时不靠延长 timeout 假修 |

## 用户问题、争议与本轮取舍

### 用户原始问题

1. 2026-08-23 的用户截图显示：升级到 `0.67.1` 后，GLM (CN) 的“添加模型”弹窗把 GLM-5.3 标成“已添加”，但 Models/模型选择仍找不到它；旧 GLM-5-Turbo 仍存在。
2. 最近 production Sentry 出现多类报错，需要区分真实产品故障、环境/退出噪声、遥测隐私违约和分类不足。
3. 用户明确要求这次把软件签名与自动更新做完整，不再只提示用户去 GitHub 手工下载。

### 必须先改判的历史结论

- B-031 虽然有 v0.67.1 的 targeted/full/build/isolated UI/CI 证据，但新截图来自**真实 0.67.1 升级用户**，因此只能保留历史证据，不能继续把用户路径写成已关闭。B-031 从 `Shipped fix` 重开为 `Shipped but production path failed`。
- 旧计划曾决定保持手动更新；用户在 2026-08-23 明确改变产品方向。该旧决定不再阻止实现 native updater，但仍保留“preview 不能进入 stable feed、签名未闭环前不能自动安装”的安全理由。
- 当前 macOS Developer ID 签名链已经存在，不应推倒重做；“彻底签名”在此基础上补 notarization/stapling、Windows Authenticode、发布资产/更新元数据身份一致性，以及 Linux 的诚实信任边界。

### 本计划采用的产品定义

- “自动更新”默认定义为：应用自动检查 stable 更新、后台下载并验证、准备完成后提示用户重启安装。首版**不强制静默重启**，活动 chat/bridge/task 不被 updater 擅自终止。
- Settings > About 保留“检查更新”；native 更新不可用或失败时退回当前按平台选择 GitHub Release 资产的手工路径，不显示假成功。
- 当前用户决策是只完成 macOS 自动安装闭环。Windows/Linux 构建代码与签名门禁保留，但 tag/prerelease 不发布其资产，手工 artifact job 关闭 official updater provenance；重新启用需新的明确决策。
- GLM 修复、Sentry P1 与 updater 可以拆成不同 patch release；不得为了等大功能而延后隐私或启动阻断修复。

## 实施前事实基线（2026-08-23 取证时）

> 本节保留开工时的仓库证据，用于解释修复为什么需要发生；它不是当前 working tree 的状态。当前实现与验证结果以顶部状态、remediation 表、Smoke Ledger 和决策日志为准。

### GLM / B-031

- `search-models` 的 catalog-only 分支用长期 stable `modelId`（GLM 当前为 `sonnet`）和变化的 wire `upstreamModelId`（`glm-5.3[1m]`）。`getPresence()` 只要本地行命中 stable 或 wire 任一身份就返回 `alreadyAdded=true`。
- 因此遗留的 `sonnet → glm-5-turbo/5.2/sonnet` 行也可能把 GLM-5.3 候选标成“已添加”，但它并不证明当前 SKU/wire 已经落库或已在 picker 可用。
- `mergeCatalogManagedModels()` 只更新 `source='catalog' && user_edited=0 && enable_source NOT IN manual_*` 的行。共享 stable id 的非 catalog-managed 行只会让该 stable slot 跳过更新；真正阻止新行插入的是“stable id 尚不存在、但 wire/upstream id 已被其他行占用”。两种路径都可能让真实旧安装卡在旧 wire，但 classifier、迁移条件和反例必须分轴设计，不能把 stable-id 命中误写成插入阻断。
- isolated fixture 基础设施首次提交于 v0.58.1，并非 v0.67.1 新增；v0.67.1 hotfix 扩展/复用了它来验证 pristine catalog、hidden、direct-wire、并发等路径，但没有用这次受影响用户的真实 row shape 完成升级 smoke。旧“已关闭”结论缺的正是这条证据。

### Sentry（上一轮只读快照，Phase 0 必须重跑）

> 当前本地环境没有 `SENTRY_AUTH_TOKEN`；下面是上一轮只读分析的 handoff 基线，不冒充 2026-08-23 的新查询。Claude 必须用本地只读 token 重跑同口径，禁止把 token 打印、提交或粘贴进聊天。

- **隐私 P1**：真实 0.67.1 JS/Electron/native event 仍出现服务端推断 geo；native minidump 可含本机绝对路径。前者说明 null-IP/project setting 至少一层未形成实际结果；后者可能绕过 JS `beforeSend`，不能用 sanitizer 单测冒充解决。
- **SQLite P1**：`CODEPILOT-DESKTOP-24` 的 `database disk image is malformed` 在 macOS/Windows 启动 `getDb()` 路径出现，上一轮看到近期约 33 次；当前没有用户可见的完整性诊断、保全、恢复/导出闭环。
- **ABI P1**：`CODEPILOT-DESKTOP-36` 在 Windows 出现 better-sqlite3 模块 ABI `143`、运行时需要 `137`。CI 现在用 `ELECTRON_RUN_AS_NODE=1` 验证 native module，而生产由 Electron Main `utilityProcess.fork()` 启动 Next，门禁没有证明同一运行形态。
- **P2 候选**：Marketplace stream 在取消后 `Controller is already closed`；Provider quick action / memory extract 报错大量落到 `unknown`；Windows `spawn EPERM` 未收敛成工具结果；Windows logoff 的 `0x40010004` / `0xC000026B` 被当产品故障；少量 GPU native crash 缺 app frame，不能直接归因或全局禁 GPU。

### 签名与更新

- `electron-builder.yml` 已有 GitHub publish provider、macOS `dmg+zip`、Windows NSIS、Linux AppImage/deb/rpm；`electron-updater` 以 `^6.8.3` 进入 production dependencies，但除 disabled stub 外没有真实调用方，启用前必须 exact pin 并按该版本合同验证。
- `electron/updater.ts` 当前明确 `DISABLED`；`electron/main.ts` 没有初始化它，`electron/preload.ts` 也没有 updater bridge。Renderer 的 `useUpdateChecker` / dialog/type 只是未接通的 latent contract，Electron 内实际走 `/api/app/updates` 手工下载 fallback。
- stable workflow 所有 package step 都使用 `--publish never`；在当前 builder/publish 配置下它仍会生成 update metadata。丢弃发生在双层白名单：macOS、Windows、Linux 三个 platform job 的 `upload-artifact.path` 都漏掉 `latest*.yml`/blockmap，central release job 的 asset glob 又再次排除它们；当前 GitHub Release 因而不是可用的 electron-updater feed。
- macOS stable 已有 Developer ID + exact Team ID fail-closed gate，但 `notarize:false`；没有 Apple notarization/stapling 证据。
- Windows NSIS/EXE 没有 Authenticode 凭据与 final signature verifier。签名主体必须跨版本稳定，否则 Windows updater 的 publisher 校验不能成立。
- 官方合同参考：[electron-builder Auto Update](https://www.electron.build/docs/features/auto-update/)、[macOS notarization](https://www.electron.build/docs/notarization/)、[Windows code signing](https://www.electron.build/docs/features/code-signing/code-signing-win/)、[Apple notarization workflow](https://developer.apple.com/documentation/security/notarizing-macos-software-before-distribution)。

## Phase 0：只读取证与实现前报告

### 用户会看到什么

- 本阶段不改产品。用户得到一份可判断“哪些是已确认根因、哪些仍是假设、先发哪个 patch”的报告。
- 明确列出需要用户提供/配置的外部凭据，但不要求在聊天里发送任何 secret。

### 本阶段明确不做什么

- 不启用 updater、不写用户数据库、不修改产品代码或外部 Sentry/GitHub 设置，也不读取任何 secret 值。
- metadata 已确认是“生成后被双层白名单丢弃”；不再把它当未知问题重复做无必要的生成 POC，只记录本次 build 的实际文件清单与丢弃链证据。

### Claude 执行清单

- [ ] 从 clean `main` 记录 commit、版本、dirty files；不覆盖用户现有改动。
- [ ] 用 Sentry bundled read-only API 固定 `project/environment/release/time range`，逐项重查 issue detail + representative events；输出只保留 short id、release、platform、时间、稳定标签和脱敏结论，不贴 raw stack/minidump/path/IP/email。
- [ ] 对隐私样本区分 Browser、Next、Electron JS、native minidump 四层；检查出站 envelope、Sentry ingest 结果与 project “Prevent Storing IP Addresses” 三份证据，不能只看代码。
- [ ] 向受影响 GLM 用户索取**脱敏只读诊断**或本地导出脚本结果：provider 只保留 `id/preset_key/provider_type`、规范化 base URL host/path；model 只保留 `model_id/upstream_model_id/display_name/enabled/source/user_edited/enable_source/sort_order/capabilities keys`。禁止 API key、完整 home path、会话正文。
- [ ] 若拿不到真实 DB，必须从 v0.67.0 及更早实际 schema/seed/migration 构造历史 fixture，不得用当前 factory 猜旧行。
- [ ] 对同一 provider 同时记录四个消费面：`GET /api/providers/[id]/models?all=1`、`POST /api/providers/[id]/search-models`、`GET /api/providers/models?runtime=...`、Settings/Composer 实际渲染。给出 stable id、wire id、显示名和 enabled/hidden 的逐行对账表。
- [x] 记录当前 builder 的实际 DMG/ZIP、ZIP blockmap 与 ZIP-only `latest-mac.yml` 清单，并固化已确认的原丢弃链：macOS/Windows/Linux 三处 `upload-artifact.path` 白名单 + central release job asset glob。其余平台真实产物仍由正式 CI 收证。
- [x] 盘点 GitHub Secrets **名称与存在性即可**；不得读取值。Apple Developer ID/Sentry 相关项原已存在，2026-08-24 补齐三项 Apple notarization Secret；Windows signer 三项仍缺并因 Mac-only 决策延后。
- [ ] 交付 `confirmed / rejected / unknown` 诊断表、建议 release 拆分、预计改动文件、测试矩阵和风险。Phase 0 未 review accepted 前，不打开 updater、不写用户 DB migration、不改外部 Sentry 设置。

## Phase 1：GLM-5.3 真实升级路径

### 用户会看到什么

- GLM-5.3 只有在当前 SKU 确实存在且可选择时才显示“已添加”。
- 可证明是旧内置目录行时，打开 Models 后安全升级为当前 GLM-5.3；真正的用户自定义冲突不会被静默覆盖，而会显示“旧目录待升级/身份冲突”的明确恢复动作。
- hidden 仍可“重新启用”；不会制造同一 upstream 的双行，也不会破坏 session pin、排序和手动能力设置。

### 本阶段明确不做什么

- 不让 Models GET 按 stable id 或 `source` 猜测迁移，不把 `user_edited=1` 粗暴等同于可覆盖或不可覆盖，也不删除/禁用 ambiguous/manual 行来制造“修好了”的表象。GET 只允许对完整历史 fingerprint 命中的系统行执行幂等 compare-and-swap。
- 不顺带改变其他 provider 的 stable identity、默认模型或推理强度合同；任何 schema 变化都必须是此迁移所需的最小 additive 变更。

### 技术取舍

- 不再用 `alreadyAdded:boolean` 压扁 SKU 状态。至少区分 `current_enabled`、`current_hidden`、`legacy_upgrade_available`、`identity_conflict`、`missing`。
- 自动迁移只允许“可证明属于历史内置目录”的行：exact provider preset + stable id + upstream/display/capability 与已发布旧 catalog fingerprint 一致。classifier 必须分别处理“stable slot 已有非 catalog-managed 旧行，因此跳过更新”和“stable slot 不存在、upstream 被其他行占用，因此阻止插入”；`user_edited=1` 不能单独证明用户所有权，也不能单独证明可覆盖，需要字段级 provenance/旧目录反证。
- 对 ambiguous/manual 行不得借 Models GET 写入。提供 preview/确认后的 replace/keep 选择；如果需要新 schema，遵守 additive + conservative backfill，并先更新 `DatabaseSchema.md`。

### 执行清单

- [x] 用已发布旧 catalog 的 gen-0 `GLM-4.7`、后续 `GLM-5-Turbo`/`GLM-5.2` 与 `GLM-4.5-Air` fingerprint 写 failing-then-passing 完整目录 fixture，覆盖 sonnet/haiku 的真实升级历史并保留各世代旧 opus 行；真实 Turbo 指纹是 upstream=`sonnet`，删除 git 历史无法证明的 upstream=`glm-5-turbo` 猜测。真实用户行仍待脱敏对账。
- [x] 把 candidate presence 从 stable-slot 命中升级为 SKU-aware 五态；stable id 相同但 wire 仍为旧世代时不再算 current。
- [x] 增加 legacy catalog classifier/迁移事务；覆盖 stable-slot、upstream occupancy、旧 upstream/display/capability、source/user_edited/enable_source 与 same-upstream direct-wire 反例。
- [x] 事务幂等、并发安全、保留非目标行数量与用户排序；不 delete、不静默 disable、不将 ambiguous 当成功。
- [x] Models API、search API、Runtime group、Settings/Composer 共享/验证同一 identity contract。
- [x] UI 对 current/hidden/legacy/conflict/missing 使用明确状态和真实恢复动作。
- [x] 更新 `ProviderManagement.md`、`ModelDiscovery.md` 与 B-031 的实现状态。
- [ ] 真实受影响用户从 0.67.1 升到候选版本后完成一次只读前后 diff + UI smoke；没有这条不得关闭 B-031。

### 本地结果

- `legacyFingerprints` 只允许 upstream/display/capabilities 全字段吻合且无用户所有权信号的旧行升级；历史 `source='manual'` 不再被单独当成用户所有权。fixture 分别使用 gen-0 与后续真实三行旧库，结果均为 sonnet/haiku 原位升级、非目标 opus 保留。
- upstream wire 被其他行占用或字段有歧义时返回 `identity_conflict`，不覆盖、不补重复 SKU；若当前 canonical SKU 已存在，则额外旧行不再把可用模型整体降级为 conflict。Add Model 不再用一个“已添加”吞掉 hidden/legacy/conflict，并实际消费冲突 model ids 给出恢复动作。

## Phase 2：Sentry P1 闭环

### 用户会看到什么

- 隐私设置与实际上报行为一致；数据库损坏时看到可恢复、可导出的诊断路径；错误 ABI 安装包在发布前被拦截，而不是交给生产用户发现。

### 本阶段明确不做什么

- 不自动删除、覆盖或重建用户唯一数据库副本；不把原始路径、IP、正文或 minidump 内容复制进报告。
- 不用 sanitizer 单测代替真实 envelope/ingest 证据，也不用 `ELECTRON_RUN_AS_NODE=1` 代替生产形态的 utility process 加载门禁。

### 2A. 隐私合同

- [ ] 若 outbound envelope 没有 `user.ip_address:null`，修三层 adapter/serialization 并以真实 transport 测试锁定；若 envelope 正确而 ingest 仍补 geo，核验并由用户批准修改 Sentry project IP 设置，代码不得冒充外部设置已改。
- [x] 裁决 native minidump：raw attachment 绕过 JS sanitizer，因此默认 U0 关闭 native dump；只有不会发布的隔离 telemetry smoke build 显式开启。JS error/source-map 能力保留。
- [x] 同步修订 `SentryTelemetry.md` ST-10、检查表、测试、决策日志与 consent 文案。
- [ ] 用正式 stable build 做 outbound envelope + Sentry ingest/project setting 三层 smoke；本地无 `SENTRY_AUTH_TOKEN`，不能用代码合同冒充 production 结果。

### 2B. SQLite 损坏

- [x] 启动前执行只读 `quick_check(1)`；失败时阻断正常 bootstrap，并用稳定、无路径 marker 让 Electron Main 停止随机端口重试。
- [x] 恢复前先时间戳备份 DB+WAL+SHM，逐文件 fsync + SHA-256/size 校验；只有完整保全后用户确认“新建数据库”才可移除原文件。
- [x] Electron recovery page 可打开备份目录、显示脱敏原因并执行用户确认的新库恢复；失败不进入空白首页。
- [x] 健康库、malformed DB、DB/WAL/SHM 保全、目标不可创建、权限/IO 分类与备份完整性已有正反例。
- [x] migration/runtime recovery 产品故障与真实 corruption 分型；busy/locked 有界重试且不永久 cache，unknown health failure 不伪装成 DB marker。
- [x] “新建空数据库”使用 fsync 后的 durable intent，且在 legacy DB 迁移前消费；旧位置陈年/损坏 DB 不会复活或形成 blocked 循环。
- [x] 初始 DB 阻断尚未赋值 `serverPort` 时，“再试一次”会重新拉起 utility 而非静默 no-op；migration/runtime wrap 前写入有界、路径/凭据脱敏根因，并进入本地日志与复制诊断。
- [x] exact DB marker 立即结束 30 秒通用 health wait；Main/utility/assets 共用 `CLAUDE_GUI_DATA_DIR` resolver。fresh-start 后重现 DB 不自动删除，用户明确选择保留当前恢复或重新校验旧备份后继续空库。
- [x] 完整时间戳恢复备份有界保留最新 10 份；只清理 strict direct-child 备份目录，不跟随 symlink、不碰手工导出目录。
- [ ] 再补真实坏 WAL、磁盘满/同步软件与 packaged recovery UI 人工 smoke；当前分类测试不冒充真实磁盘故障。

### 2C. better-sqlite3 ABI

- [x] 本机实际复现 Electron ABI 143 的 `better-sqlite3` 被 Node ABI 127 测试拒绝；验证 native rebuild 产物不能跨 runtime 混用，并在测试前恢复 Node ABI。
- [x] 新 package gate 从**实际 Electron Main → utilityProcess.fork → packaged Next `/api/health`/最小 DB query**加载 better-sqlite3；本机 macOS arm64 报告 ABI 143 且 health 通过。
- [ ] macOS arm64/x64、Windows x64、Linux x64/arm64 都记录 `process.versions.modules` 与实际 `.node` 加载结果；错误产物 fail closed，不上传 Release。

## Phase 3：Sentry P2 分类与可恢复错误

### 用户会看到什么

- 取消安装、系统策略拒绝进程和 Provider 可行动错误不再产生二次崩溃或模糊 `unknown`；真正的产品故障仍保留 exactly-once 诊断。

### 本阶段明确不做什么

- 不全局吞掉 EPERM/EACCES、Windows 退出码或 GPU crash；只有证据充分的 expected lifecycle/user-action 才降噪。
- 不重写已经把 spawn error 收敛为工具结果的 `src/lib/tools/bash.ts`，本阶段补的是跨入口 stream 终态所有权和统一错误分类。

- [x] `src/app/api/skills/marketplace/install/route.ts`、`remove/route.ts`、`src/app/api/cli-tools/[id]/install/route.ts` 与 `src/app/api/media/jobs/plan/route.ts` 四条同构 stream 一并改为 single-owner writer + request/timeout cancellation；cancel-before-first-byte、mid-stream cancel、double-finalize 已有反例。
- [x] `StreamSession.md` 新增 server-produced Web Stream 惯用法：cancel 先认领 terminal 再 kill/abort，所有 data/exit/error callback 只经 `SingleOwnerStreamWriter`，禁止 attach 后裸写 controller。
- [ ] Provider quick action / memory extract 先通过 root-cause normalizer 分类：缺 key/4xx/模型不支持保持用户可见但 0 Issue；retry exhausted 的 5xx/DNS/timeout exactly-once；unknown 保留安全 stack 与 `needs_classification=yes`，不上传 response body。
- [x] 保留 `src/lib/tools/bash.ts` 已有的结构化 spawn failure，在 `src/lib/error-classifier.ts` 只把带 spawn/exec 上下文的 EPERM/EACCES 归 `EXECUTION_PERMISSION_DENIED`；bare fs EPERM/EACCES 保持产品故障、可上报。
- [ ] Windows logoff 的 `0x40010004` / `0xC000026B` 只在已证明 session shutdown/logoff 时归 expected lifecycle；同码非退出场景仍保留故障，不能全局 ignore。
- [ ] GPU native crash 保留 platform/driver/version cohort；没有 app frame/可复现前不全局 `disableHardwareAcceleration()`，也不把 Electron patch upgrade写成精确根治。

## Phase 4：全平台发布身份

### 用户会看到什么

- 本轮 GitHub Release 只提供 macOS 安装包；通过 CI 后应具备 Developer ID、公证、staple 与 Gatekeeper 证据。Windows/Linux 不在本轮下载列表出现。

### 本阶段明确不做什么

- 不在缺少真实 signer/notarization credential 时宣称 Release ready，不读取或提交私钥，也不以本地 ad-hoc 签名替代官方 CI 证据。
- 不承诺有效 OV 签名能立刻消除 SmartScreen 信誉提示，不让 Linux 应用在尚无可信安装模型时静默提权。

### macOS

- [x] 保留现有 Developer ID + exact Team ID fail-closed；启用 electron-builder notarization，Apple API key secret 只进入 certificate-backed macOS CI step。
- [x] 实现 final artifact verifier：`codesign --deep --strict`、Team ID、`spctl`、app/DMG `stapler validate`，以及 ZIP 解包后的 app ticket。
- [x] 仓库已存在 Developer ID 证书/密码/Team ID；2026-08-24 另配置 `APPLE_NOTARIZATION_KEY_BASE64`、`APPLE_API_KEY_ID`、`APPLE_API_ISSUER` 三个 Secret。只核对名称/更新时间，未读取或记录值。
- [ ] 用正式 Apple credentials 跑 arm64/x64 DMG/ZIP CI 证据。用户选择不做旧版本 → notarized 候选的 Safe Storage/keychain 实机 smoke；该风险保留，不冒充通过。

### Windows（本轮延后，不进入 Release）

- [ ] 未来重新发布 Windows 时，用户在 OV/EV PFX、HSM 或 Azure Trusted Signing 中选择可持续 CI 方案并创建真实 secret；当前 workflow 只保留 PFX 合同与手工 artifact job。
- [x] 配置 PFX、RFC3161 timestamp、稳定 publisher subject 与 `forceCodeSigning` fail-closed；证书缺失/subject 漂移时 package job 失败。
- [x] 实现 `Get-AuthenticodeSignature` verifier，只覆盖 CodePilot 自有 installer 与顶层 `win-unpacked/CodePilot.exe`，要求 `Status=Valid`、exact subject 和 timestamp；第三方 helper 保留自身 publisher，不递归误阻断。
- [x] Windows PFX 密码不写 `$GITHUB_ENV`，只注入 electron-builder package step；证书路径与非敏感 publisher subject 可跨 verifier step 使用。
- [x] 文档记录 SmartScreen 信誉边界：有效 OV 签名不等于第一天零提示。
- [ ] 未来重新启用 Windows Release 时，用正式 signer 跑 verifier 与 clean-machine 安装/升级 smoke。

### Linux / 资产范围

- [x] 明确 AppImage、deb、rpm 的信任边界；未有受信 GPG repository/manifest verifier 前只提供检测/手工 fallback，不静默提权安装。
- [x] Release workflow 生成 checksum 并用 `actions/attest@v4` 对 immutable artifact graph 做 attestation；central job 不重算或混入二进制。
- [x] tag/prerelease 发布依赖收窄到 Mac；Windows/Linux 只保留手工 artifact job 且 `CODEPILOT_OFFICIAL_UPDATE_BUILD=0`，central verifier 反向拒绝非 Mac 资产混入。
- [x] 写入密钥轮换与丢失 runbook：新旧 signer overlap、Windows publisher compatibility、Apple Team ID 不变、撤销/过期处理；私钥不得进入 repo/artifact/log/cache。

## Phase 5：更新产物与 channel

### 用户会看到什么

- macOS 能从对应 channel 获取同一 immutable build 生成的安装包、metadata 与 blockmap；缺失、错配或混入非 Mac 资产时发布直接失败，不会给用户下发半套更新。

### 本阶段明确不做什么

- 不手写 electron-builder metadata，不使用或发布后修改 `stagingPercentage`，也不通过移动 tag、复用版本或同版本替换二进制来“回滚”。
- 不把 preview artifact-only workflow 接到 stable feed；stable 晋级前的验证和 stable 全量发布是两个需要独立证据/授权的动作。

- [x] exact pin `electron-builder@26.8.1`；`dmg.writeUpdateInfo=false` 后，本地 macOS arm64 `--publish never` 真实生成 DMG/ZIP、仅 ZIP blockmap 与 ZIP-only `latest-mac.yml`。DMG 是手工 bootstrap 资产，不进入 updater hash graph；其余平台由 source contract + 正式 CI 收证。
- [x] 历史全平台 asset allow-list 修复保留；2026-08-24 按用户决策把 tag/prerelease central graph 收窄为 macOS，并用 `stable/preview + macos` verifier target 锁定。
- [x] stable 与两条 preview workflow 的 native arm64+x64、universal updater、公证拆成三个独立 bounded step，各自 45 分钟，不让第二次 package 吞掉共享预算。
- [x] central release job 在上传前验证版本、文件名、sha512、size、架构、URL、blockmap 与 checksum 覆盖，缺项/错 hash fail closed；完整 immutable graph 先上传/恢复同 tag draft，全部成功后才一次转为公开 stable/prerelease。
- [x] mac verifier 拒绝 metadata 引用 DMG，并要求 metadata 恰好一条 universal ZIP entry及其 hash/size/blockmap；DMG 后置 staple 不再改写任何 updater metadata 所引用的字节。
- [x] macOS 保持 ZIP updater target；Windows/Linux 不随 tag 发布，只有显式手工 artifact job。
- [x] stable 只发布 `latest-mac.yml`；preview 使用独立 prerelease 与 `preview-mac.yml`，不会覆盖 stable feed。
- [x] preview macOS 同时生成 arm64/x64 手工 DMG 与 universal updater ZIP；central preview verifier 强制 3 份 DMG/ZIP、各 ZIP blockmap 与 universal metadata target，Intel 不再等 stable 才首次出包。
- [x] 同步 `docs/rules/release.md` 与 `docs/guardrails/Release.md` 的资产/验证/恢复合同。
- [x] 明确不使用 `stagingPercentage`；stable 坏版本只通过更高 patch 恢复，禁止 metadata/tag/version mutation。
- [x] stable/preview workflow 顶层降为 `contents: read`，Release/OIDC/attestation 写权限只授予最终发布 job；Mac-only 两个 channel 对称拒绝 Windows/Linux metadata/package 混入。

## Phase 6：Main-owned updater 与 UX

### 用户会看到什么

- 支持的平台会后台检查和下载可信更新，准备好后由用户决定何时重启；活动 chat/bridge/task 不会被强制打断，失败时仍可继续使用旧版或手工下载。

### 本阶段明确不做什么

- 不让 Renderer 决定 feed/channel/签名信任，不做强制静默重启，也不把 preview/fork/dev 包误接到 stable updater。
- 不沿用“类型里存在就当 preload 已实现”的旧假设；Main/preload/types/hooks 必须在同一合同下完成或删去不可达分支。

- [x] exact pin `electron-updater@6.8.3`，与 `electron-builder@26.8.1` 固定配对。
- [x] Main 实现 idle/checking/available/downloading/downloaded/installing/error 单一状态机，并发 check/download 去重，窗口重建可重放 snapshot。
- [x] 代码仍只允许 packaged + official provenance + (stable/preview) + macOS/Windows 初始化；本轮只有发布的 Mac 包带 provenance，Windows 手工 artifact 强制为 `0`，dev/fork/local/Linux honest no-op。Main 固定 stable→`latest*`、preview→`preview*`；Renderer 不能传 feed/channel/path/options，也不调用 `setFeedURL`。
- [x] stable 固定 `allowPrerelease=false`，preview 固定 `allowPrerelease=true`，两者 `allowDowngrade=false`；snapshot 含 app version、channel、package type、支持原因与低基数错误码。
- [x] 裁决旧 type 为 stale scaffold，并在 Main/preload/types/hooks 同步换成真实窄 bridge。
- [x] preload 只暴露 status/check/download recovery/install 与 status event；Main 验证 sender/window 和状态。
- [x] 启动 30–120 秒抖动检查、8 小时复查、自动下载、失败退避与低噪声日志已实现。
- [x] install 前从服务端持久事实检查 active chat/bridge/task；chat 要求 `running` / `streaming` / `waiting_permission` active status 与未过期 runtime owner 同时成立，既保护正在输出的流又排除 crash residue；activity API 不可用时 fail closed，正常退出仍可安装，手工下载 fallback 保留。query→quit 跨入口原子准入明确留 active #88。
- [x] About/Dialog 以 Main snapshot 为真源，展示进度、目标版本、准备/错误/fallback；release notes 有长度上限并沿用安全渲染边界。
- [x] macOS 安装前监听 native `before-quit-for-update`，先解除 close-to-hide；否则不得把窗口隐藏/状态卡住冒充安装成功。
- [x] installer 未进入真实 teardown 时 15 秒回滚临时 quit latch，恢复窗口/托盘与 downloaded + `install_failed`；downloading/downloaded/installing 期间拒绝并发 check。
- [x] manual fallback 在最新 Release 缺当前平台资产时返回 `platformAssetMissing`，Dialog/About/Overview 明确“该版本无此平台安装包”且不提供假下载直链；下载/已下载/安装阶段的检查按钮禁用并显示“更新进行中”。
- [x] 新建 `Updater.md` 并更新 guardrail 索引。

## Phase 7：验证、渠道晋级与恢复

### 用户会看到什么

- 0.67.1 用户仍需手工安装首个 updater-capable macOS 版本；后续版本才具备原生自动更新。用户本轮选择跳过 RC-A → RC-B 实机验收，发布汇报必须明确这项风险。

### 本阶段明确不做什么

- 不把 0.67.1 手工下载安装冒充 native auto-update，也不把未执行的 clean-machine/RC-A → RC-B 验收写成通过。
- 不做不可审计的 10%/50%/100% 百分比放量；stable 全量发布后不修改 metadata 或复用 tag/version，恢复只发更高 patch。

### 自动门禁

- targeted：GLM identity/DB/routes/UI、Sentry sanitizer/classifier/utility、updater state/IPC/channel/release assets、signing policy。
- full：`npm run test`。
- production：`npm run build` + `npm run electron:build`。
- package CI：macOS arm64/x64 + universal updater；真实 Electron utility process health、0 source map、签名/公证、metadata/hash 引用。
- 文档：`npm run lint:docs-drift`、`npm run lint:hooks`、`git diff --check`。

### 真实升级矩阵

| From → To | 平台/安装形态 | 必测场景 | 通过条件 |
|---|---|---|---|
| 0.67.1 → RC-A | macOS arm64/x64 DMG/ZIP | **手动**下载/安装 bootstrap、Gatekeeper、Safe Storage、数据/会话保留 | RC-A 启动且 updater 可用；无重复 keychain 授权，旧数据完整；证据不标成 auto-update |
| RC-A → RC-B | macOS arm64/x64 | 后台检查/下载、用户确认重启安装、签名/公证/metadata 验证 | RC-B 启动且版本正确；失败可保留 RC-A 或走手工 fallback |
| current = latest | macOS | 启动/手工检查 | 不下载、不弹新版本 |
| preview installed | macOS | 检查 stable/preview channel | 不覆盖 stable feed；按明确 channel policy 升级 |
| wrong hash/wrong signer/missing metadata/offline | macOS | 失败与重试 | 拒绝安装、旧版可用、错误可行动且不泄漏 |
| active chat/bridge/task | macOS | 更新已下载 | 不强退；稍后/下次启动可完成 |

### 渠道晋级与恢复

- 推荐路径仍是先在独立 internal preview channel 验证后再晋级 stable；但用户明确选择本轮跳过实机 RC 验收。最终 stable tag 仍需用户单独授权，CI 必须 fail closed 完成签名、公证、ABI 与资产图门禁；未跑的用户机升级风险在发布汇报中保留。本轮不写入或修改 `stagingPercentage`，也不把 preview 二进制改名后混入 stable。
- updater 本身首个 stable 必须保留手工下载 fallback。更新失败不能阻断应用启动或设置页访问。
- stable 发布前发现问题：阻止渠道晋级并保留 preview 证据。stable 已全量发布后发现严重问题：保留原 tag/Release/metadata，修复后发更高 patch；禁止删 tag、复用版本、修改已发布 metadata 或用旧二进制覆盖资产。

## Required checks（Claude 交付必须逐项回写）

| ID | Required check | 通过证据 |
|---|---|---|
| RC-GLM-LEGACY | 真实/历史 0.67.1 row shape 可复现；四个消费面一致 | 前后对账 + failing-then-passing fixture + 真实受影响用户 smoke |
| RC-GLM-OWNERSHIP | pristine legacy 自动升级，manual/ambiguous 不静默覆盖，hidden/排序/session pin 保留 | 正反例 DB transaction tests |
| RC-SENTRY-PRIVACY | official stable JS event 无 IP/Geo；native dump policy 与 consent 一致且无虚假 U0 承诺 | outbound envelope + project setting + ingest event 三层证据 |
| RC-DB-RECOVERY | malformed DB 不循环崩溃、不自动删除，备份/诊断/恢复/导出可用 | corrupt/WAL/disk/permission fixtures + packaged UI smoke |
| RC-ABI-PROD | 真正 Main→utilityProcess 加载 packaged better-sqlite3 | Mac arm64 runner + Intel universal ZIP 的 production-shaped health evidence |
| RC-SENTRY-CLASS | expected/user-action 0 Issue；transient/product fault exactly-once | 阳性/零事件共享 carrier tests + 新 stable read-only cohort |
| RC-MAC-TRUST | Developer ID、exact Team ID、notarized、stapled、Gatekeeper accepted | final app/DMG/ZIP CI verifier；真实跨版本 smoke 由用户明确跳过，不标通过 |
| RC-WIN-TRUST | 本轮延后 | Windows 不进入 tag/prerelease，手工 artifact provenance=0 |
| RC-UPDATE-ASSETS | Mac installer/metadata/blockmap 同构建同版本，stable/preview 隔离 | Mac upload/release glob source-contract audit，以及 missing/mismatch/non-Mac negative tests |
| RC-UPDATE-UX | 自动检查/下载、用户确认重启、活动任务不被强杀、fallback 可用 | Main state/IPC tests + packaged `RC-A → RC-B` UI smoke |
| RC-PROMOTION-RECOVERY | preview 可阻断 stable 晋级；stable 全量发布后坏版本只用更高 patch 恢复 | preview → stable 与 higher-patch 恢复演练；无 metadata mutation/tag/version reuse |
| RC-FULL | Tier 2 full/build/package/docs gates 全通过 | 命令、结果、commit、已知 flake/未跑项如实记录 |

## Human gates / 外部依赖

- **Sentry**：执行者本地设置只读 `SENTRY_AUTH_TOKEN`（`project:read/event:read/org:read`）；不在聊天或 artifact 中传 token。
- **GLM**：受影响用户提供脱敏诊断或在本机执行只读导出；任何写入/修复前先备份并由用户确认。
- **Apple**：`APPLE_NOTARIZATION_KEY_BASE64`、`APPLE_API_KEY_ID`、`APPLE_API_ISSUER` 已于 2026-08-24 配置；只核对名称/更新时间，私钥值未输出。首次正式 CI 仍是凭据可用性的证据。
- **Windows**：本轮明确延后；没有 signer 时不得把 Windows 资产加入 Release 或开启 official auto-install。
- **发布**：push、tag、preview Release、stable 全量晋级与 Sentry project 设置变更都需要用户单独授权；本计划不使用百分比放量，也不构成任何外部变更的自动授权。

## Claude → Codex 交付合同

Claude 每个 Phase 的 handoff 必须包含：

1. `runId / branch / commit / dirty files`；
2. 用户问题、争议、采用/否决方案与原因，不能只贴执行清单；
3. confirmed/rejected/unknown 根因表；
4. changed files 与行为差异；
5. required checks 对账，附测试/构建/smoke 原命令和结果；
6. 真实凭据/外部设置/平台未覆盖项；
7. 风险、回滚与下一步；
8. 是否请求 Codex review。P1/P2 finding 没有修复、验证或 tracker 条目不得口头关闭。

## Smoke Ledger

| Date | Runtime | Provider / Channel | Version / Model | 凭据形态 | 场景 | Result | Evidence |
|---|---|---|---|---|---|---|---|
| 2026-08-23 | user production screenshot | GLM (CN) | CodePilot 0.67.1 / GLM-5.3 | 用户现有套餐；未读取 key/DB | Add Model 显示 GLM-5.3“已添加”，Models/模型选择仍找不到 | ❌ B-031 reopened | 对话附件；旧 isolated smoke 不能关闭该真实升级路径 |
| 2026-08-23 | local repository audit | stable release pipeline | 0.67.1 | 无签名 secret读取 | updater/signing/release asset 静态核对 | ✅ 只读事实基线 | native updater disabled；preload/Main 未接；mac notarize false；Windows 未签；metadata 被三处 platform artifact allow-list 与 release asset glob 双层丢弃 |
| 2026-08-23 | local Node test runtime | GLM/DB/Sentry/stream/updater/release contracts | candidate working tree | 隔离测试 DB；无真实 provider/Sentry/signing secret | typecheck + harness boundary + 全量 unit | ✅ 5284 pass / 0 fail / 1 skip | `npm run test`，1222 suites / 5285 tests；第二轮高风险定向 79/79；最终审计定向 56/56；Node ABI 已在 package smoke 后恢复 |
| 2026-08-24 | local Node test runtime | GLM published history + DB startup recovery | candidate working tree | 隔离测试 DB；无真实 provider/signing secret | gen-0/后续三行迁移、identity conflict、无端口 retry、脱敏 migration diagnostic；随后标准全量 | ✅ 49/49 targeted；5289 pass / 0 fail / 1 skip | `npm run test`：1222 suites / 5290 tests；79/79 与 56/56 是上一日两条不同范围的独立命令，不是同一结果漂移 |
| 2026-08-24 | local Node test runtime | P3 closeout：updater/GLM/DB/release/stream | candidate working tree | 隔离测试 DB；无真实 signer/provider | 13 项 P3 行为/合同、preview Intel/universal 资产负向门禁；随后标准全量 | ✅ 72/72 targeted；5297 pass / 0 fail / 1 skip | `npm run test`：1222 suites / 5298 tests；typecheck + harness boundary 均包含在完整命令内 |
| 2026-08-24 | local Node test runtime | P3 second closeout：GLM 409 / DB generation+recovery / stream routes / updater activity | candidate working tree | 隔离测试 DB；无真实 signer/provider | typed 409+i18n/回读、跨文件备份撕裂与内容去重、busy/diagnostic/UI 语义、四 route writer 接线、stale snapshot/live owner；随后标准全量 | ✅ 70/70 targeted；5302 pass / 0 fail / 1 skip | `npx tsc --noEmit`；5 个定向文件 70/70；`npm run test`：1222 suites / 5303 tests。query→quit 原子 fence 不在此冒充通过，见 #88 |
| 2026-08-24 | local Node test runtime | updater streaming activity P2 closeout | candidate working tree | 隔离测试 DB；无真实 signer/provider | `running` / `streaming` / `waiting_permission` active-status 与 live/expired owner 正反例；随后标准全量 | ✅ 6/6 targeted；5303 pass / 0 fail / 1 skip | `npx tsx --test --import ./src/__tests__/db-isolation.setup.ts src/__tests__/unit/updater-contract.test.ts`；`npm run test`：1222 suites / 5304 tests；`lint:docs-drift` 与 `git diff --check` 通过 |
| 2026-08-24 | GitHub Secret name audit + local workflow contract | macOS stable/preview | candidate working tree | 只核对 Secret 名称/更新时间；私钥不输出 | 三项 notarization Secret 写入；tag/prerelease Mac-only；verifier 接受完整 Mac graph 并拒绝混入 Windows asset；标准全量与本地 production build | ✅ Secrets configured；14/14 targeted；5304 pass / 0 fail / 1 skip；Build passed | `npm run test`：1222 suites / 5305 tests；`CODEX_DISABLED=1 npm run build`：136 routes；`node scripts/build-electron.mjs` passed。尚未 push/tag/Release，也未运行正式凭据 CI，不算签名/公证或跨版本 smoke |
| 2026-08-24 | local final-review closeout | macOS-only stable/preview + Windows/Linux fallback | candidate working tree | 无真实签名 secret；只使用 fixture 与 source contract | 非 Mac 缺资产 API/UI/i18n；Mac-only 禁入表；恰好一条 universal ZIP；发布权限与 draft 可见性；下载期检查反馈；标准全量与双构建 | ✅ 38/38 targeted；5308 pass / 0 fail / 1 skip；Build passed | 6 个定向文件 38/38；`npm run test`：1222 suites / 5309 tests；`CODEX_DISABLED=1 npm run build`：136 routes；`node scripts/build-electron.mjs` passed；docs/hooks/diff/YAML 门禁通过。正式凭据 CI 与跨版本 smoke 仍未跑 |
| 2026-08-24 | local release preparation | macOS stable | v0.67.2 | 本地无签名 secret；CI 凭据值不回读 | Release Notes/版本三元组、完整测试、Next production、Electron bundle、pre-commit 重跑 | ✅ 5308 pass / 0 fail / 1 skip；Build passed | `4d1a7e7e`；`npm run test` 与 pre-commit 均为 1222 suites / 5309 tests；136 routes；Electron bundle passed。用户已授权 push/tag；正式签名、公证、attestation 与公开 Release 仍以 tag CI 为准 |
| 2026-08-23 | local production build | local/unpublished | candidate working tree | 无正式签名 secret | Next production + Electron build | ✅ Build passed | 136 pages；Electron bundle passed；已有 NFT/Turbopack `claude-home-shadow` warning 保留为非本轮 warning |
| 2026-08-24 | local production build | local/unpublished | candidate working tree after fix-round closeout | 无正式签名 secret | 收口代码的 Next production + Electron bundle | ✅ Build passed | `npm run build`：136 routes；`node scripts/build-electron.mjs` passed；同一既有 NFT/Turbopack warning，无新增 build error |
| 2026-08-24 | local production build | local/unpublished | candidate working tree after P3 closeout | 无正式签名 secret | shared data-dir/recovery/updater/preload + preview workflow 最终构建 | ✅ Build passed | `npm run build`：136 routes；`node scripts/build-electron.mjs` passed；仍只有既有 NFT/Turbopack `claude-home-shadow` warning |
| 2026-08-24 | local production build | local/unpublished | candidate working tree after P3 second closeout | 无正式签名 secret | GLM renderer/API、DB recovery diagnostic、Main updater activity 与 Electron import/build | ✅ Build passed | `npm run build`：136 routes；`node scripts/build-electron.mjs` passed；同一既有 NFT/Turbopack `claude-home-shadow` warning |
| 2026-08-23 | local macOS arm64 ad-hoc package | stable-shaped local package | 0.67.1 | `CODEPILOT_ALLOW_ADHOC_SIGNING=1`；无 notarization | packaged Next + Main→utilityProcess→health→SQLite ABI | ✅ Local ABI passed；metadata 旧形状随后被 review 打回；不算 trust evidence | 0 source maps；health OK，Electron ABI 143。该次 package 仍有 DMG update entry，不能作为修复后的 asset graph 证据；无 Developer ID/notary credential |
| 2026-08-23 | local electron-builder shape probe | macOS arm64 / unpublished | 0.67.1 | ad-hoc；关闭 native rebuild/hooks，仅验证 builder 输出形状 | `dmg.writeUpdateInfo=false` 后真实 `--publish never` DMG/ZIP metadata | ✅ ZIP-only graph passed；不算签名/ABI smoke | `latest-mac.yml` 仅含 `CodePilot-0.67.1-arm64.zip`；ZIP blockmap 存在，DMG 存在而 DMG blockmap 不存在；`js-yaml` 直接依赖可解析 |
| 2026-08-23 | local docs/workflow gates | stable/preview | candidate working tree | 无外部凭据 | docs drift、hooks、diff、workflow/builder YAML parse | ✅ Passed | `npm run lint:docs-drift`、`npm run lint:hooks`、`git diff --check`、4 份 YAML parse |
| _待跑_ | Sentry API | official production | codepilot@0.67.1 | 本地只读 token | 隐私/SQLite/ABI/stream/provider/logoff/GPU cohort 重查 | 📋 | 本 turn 无 token，不复用聊天推断冒充新查询 |
| _待跑_ | affected upgrade DB + packaged UI | GLM (CN) | 0.67.1 → RC / GLM-5.3 | 脱敏 DB + 用户现有套餐 | legacy row迁移、Models/Search/Composer/turn | 📋 | 必须登记前后 row diff，不打印 key |
| _待跑_ | official macOS CI + clean Macs | internal preview | 0.67.1 → RC-A | Developer ID + Apple notarization secrets | 双架构手动 bootstrap、签名、公证、staple、Gatekeeper、数据/keychain 保留 | 📋 | 0.67.1 无 native updater；此行不得标成自动更新，local ad-hoc 不可替代 |
| _待跑_ | official macOS CI + clean Macs | internal preview | RC-A → RC-B | Developer ID + Apple notarization secrets | 双架构真实自动检查/下载/重启安装 | 📋 | 必须从安装后的 RC-A 触发，不以手工覆盖安装替代 |
| _待跑_ | official Windows CI + clean Windows | internal preview | 0.67.1 → RC-A | Windows signing secret/service | Authenticode + per-user NSIS 手动 bootstrap + custom dir/data 保留 | 📋 | 0.67.1 无 native updater；unsigned/local package 不可替代 |
| _待跑_ | official Windows CI + clean Windows | internal preview | RC-A → RC-B | Windows signing secret/service | Authenticode + per-user NSIS 真实自动更新 + custom dir | 📋 | 必须从安装后的 RC-A 触发，不以手工覆盖安装替代 |

## 决策日志

- 2026-08-23：真实 0.67.1 用户截图推翻“B-031 用户路径已关闭”；保留 v0.67.1 的代码/测试/隔离 UI/发布历史，但状态改为 reopened，禁止删除旧证据或把新反馈解释成“用户没刷新”。
- 2026-08-23：GLM 的核心争议不是再加一条 catalog，而是 stable slot 与 vendor SKU/wire 身份被同一个 `alreadyAdded` 压扁；修复先建立 SKU-aware 状态，再裁决可证明 legacy 与真正 user-owned 冲突。
- 2026-08-23：Sentry 上一轮结论只作为 handoff baseline。本 turn 因无只读 token不重查；Claude Phase 0 必须调用 API并交叉验证 release/event/code，不能把 issue count 写成永久事实。
- 2026-08-23：native minidump 若天然包含路径/内存内容，就不能继续靠 JS sanitizer 声称 U0 无 PII；必须调整默认行为或新增独立 consent，这是隐私合同问题，不是文案美化。
- 2026-08-23：用户明确重启自动更新方向。默认采用后台检查/下载 + 用户确认重启，不做强制静默重启；完整性和用户任务安全优先。
- 2026-08-23：先签名/公证，再自动安装。macOS 复用既有 Developer ID 链补 notarization；Windows 未有稳定 publisher 前不能开启 auto-install；Linux 未有可信安装模型前诚实降级。
- 2026-08-23：更新 metadata 不手写。沿用 electron-builder 生成的 `latest*.yml`/blockmap，并在 central release job 原子审计/发布；preview 继续与 stable 分离。
- 2026-08-23：接受 plan review P1。0.67.1 没有 native updater，真实矩阵拆成 `0.67.1 → RC-A` 手动 bootstrap 与 `RC-A → RC-B` 自动更新；二者证据不得混写。
- 2026-08-23：本轮放弃 GitHub Releases 的百分比放量，不使用 `stagingPercentage` 或发布后手改 YAML。采用 internal preview 完整验收 → 用户授权 stable 全量晋级；全量后的恢复只发更高 patch。
- 2026-08-23：`--publish never` 不是 metadata 缺失根因；已确认 macOS/Windows/Linux 三处 artifact upload 白名单与 central release glob 双层丢弃，Phase 5 必须四处同改并以 source-contract test 锁定。
- 2026-08-23：GLM legacy classifier 分开处理 stable-slot skip 与 upstream occupancy；不能再用“共享 stable id 阻止新行插入”指导迁移。
- 2026-08-23：若 native dump 改为默认关闭/独立 opt-in，必须同步修订 Sentry ST-10、检查表、测试与决策日志；guardrail 与代码不得分叉。
- 2026-08-23：启用 updater 前先 exact pin 依赖，并把 `src/types/electron.d.ts` 的旧 bridge 视为 stale scaffold；Phase 6 以 Main-owned snapshot/IPC 对齐 Main、preload、types、hooks，同时新建 Updater guardrail。
- 2026-08-23：最终自审发现 preview 包若 no-op 就无法执行 RC-A → RC-B 门禁；改为 Main-owned stable→latest、preview→preview feed 映射。Preview Release tag 同步从 electron-updater 无法按 semver 识别的 `preview-X.Y.Z` 改为等于 package version 的 `X.Y.Z-preview.N`；若误用 `vX.Y.Z-preview.N`，stable workflow 会在最前置 source gate fail closed，不能创建 GitHub Latest。
- 2026-08-23：用户随后明确授权 Codex 在当前任务内完成实现，但要求不启动 loop。该授权覆盖本地代码、测试、workflow 与文档闭环；仍不授权 push、tag、Release、采购证书、写 GitHub/Sentry 外部设置或处理真实用户数据库。
- 2026-08-23：本地 package smoke 证明 Electron Main→utilityProcess 的 ABI 143 路径可加载，同时也实际复现了 Electron ABI 产物不能被 Node ABI 127 测试加载；验证后已 `npm rebuild better-sqlite3` 恢复 Node ABI，当前最终全量为 5284/0/1。这个事件作为 ABI 门禁的正反证据保留。
- 2026-08-23：implementation review 的 5 个 P1 当时记为“全部修复”，但 2026-08-24 修复轮 review 证明 GLM 项仍漏 gen-0 `GLM-4.7`，该声明属于状态失真。其余四项（mac metadata ZIP-only、mac 安装解除 close-to-hide、migration/runtime 不冒充 corruption、fresh-start intent 优先于 legacy migration）保持已验证；GLM 项先回退为部分，再以发布历史、完整三行 fixture 和 conflict 行为断言补齐。
- 2026-08-24：补齐 gen-0 sonnet `GLM-4.7` 指纹；修复 `serverPort=null` 时数据库“再试一次”静默 no-op；migration/runtime 根因以本地脱敏 marker 进入日志与复制诊断，不进入高基数 startup code；新增 `identity_conflict` fail-closed 行为断言。收口定向 49/49；标准 `npm run test` 1222 suites / 5290 tests，5289 pass / 0 fail / 1 skip。
- 2026-08-24：上一轮 `79/79` 与最终审计 `56/56` 是两条不同测试集的独立结果，Smoke Ledger 分列口径。13 项低优先级边界先登记 tech-debt #87；首次以 72/72、5298 全量声称“13/13”随后被复审证明对 ⑪⑫⑬ 不成立，因此先纠正状态再修，不再沿用该关闭口径。
- 2026-08-23：保留 preview native updater 作为有边界的产品选择，而不是恢复 no-op。理由是 RC-A→RC-B 本身就是自动更新的关键验收路径；只允许 official workflow provenance bit + 官方 repo 构建启用，普通 local/fork 默认 no-op。稳定版 workflow 另外拒绝 `vX.Y.Z-preview.N`，避免 prerelease 污染 GitHub Latest。
- 2026-08-23：DMG 是 notarized/stapled 的手工 bootstrap 资产，macOS native updater 只消费 signed/notarized ZIP。`dmg.writeUpdateInfo=false` 不是绕过验证，而是避免 staple 在 metadata 生成后改写 DMG 字节；central verifier 反向拒绝 DMG entry。真实 builder 形状探针已确认 ZIP-only metadata + ZIP blockmap，正式 Developer ID/notary/RC smoke 保持 human gate。
- 2026-08-23：DB startup 错误合同拆分为 corruption、transient busy、migration 与 runtime-recovery。只有 corruption 可展示备份/新建空库；busy 可重试且不持久 block，migration/runtime 不声称数据坏或已备份。fresh-start 使用 durable marker 防止旧路径 DB 在 relaunch 后复活。
- 2026-08-24：P3 closeout 采用“收窄破坏权、扩大可验证性”的统一策略：installer handoff 可回滚；GLM presence 以唯一 enabled current wire 为真；fresh-start 后重现 DB 保留并让用户二选一；恢复与 utility 共用 data-dir，备份要求同一 generation 且相同内容世代去重后最多保留 10 份；Windows 只校验 CodePilot 自有 EXE、密码限 package step；preview/stable 都产出双架构 + universal，三段 CI 各有独立 timeout；callback/subprocess stream 统一 single-owner 并逐 route 钉接线。
- 2026-08-24：第二次 P3 收口把 updater 无更新旧字段、runtime_status 残留，GLM POST 409 测试/i18n/回读，以及 DB 跨文件撕裂、preservation-failed 操作语义、持久故障文案、rw-open busy 备份、unknown health 裸 marker 一并闭环。activity snapshot 到 quit 的原子性需要 chat/bridge/task 共用 durable admission lease，局部二次检查仍有 TOCTOU，因此独立登记 #88，保持 P3 非阻塞但不声称已修。
- 2026-08-24：第二次 P3 复审发现 updater 的 live-owner 判定漏掉真实 `streaming` 状态，属于会强杀活跃流的 P2，不接受当时的“可以 commit”结论。现已补齐 `running` / `streaming` / `waiting_permission` 三种活跃态，独立 `streaming + live lease` 行为测试 6/6，标准全量 1222 suites / 5304 tests（5303 pass / 0 fail / 1 skip），恢复 commit-ready。其余低优先级恢复/更新尾项统一登记 #89，不口头丢弃。
- 2026-08-24：实现提交为 `377c54bb`（`fix: harden production recovery and updates`）。pre-commit 再次通过 lint-staged/docs drift、typecheck、harness boundary 与完整 1222 suites / 5304 tests（5303 pass / 0 fail / 1 skip）；无 push/tag/Release，正式凭据与真实跨版本升级门禁仍保持未完成。
- 2026-08-24：用户明确把本轮发布范围收窄为 macOS 自动更新，Windows/Linux 暂不发布。stable tag 与 preview prerelease 只依赖 Mac job；central verifier 新增 `macos` target 并拒绝非 Mac metadata/installer；Windows/Linux 只保留手工 artifact job且 official updater provenance=0。重新纳入平台需新的明确决策。
- 2026-08-24：用户提供 App Store Connect Team API key 的本机路径与非秘密标识符，并授权写入仓库 Secrets；已配置 `APPLE_NOTARIZATION_KEY_BASE64`、`APPLE_API_KEY_ID`、`APPLE_API_ISSUER`，本地 `.p8` 权限收紧为 `0600`。计划不记录 key 内容，GitHub 也不允许回读值。
- 2026-08-24：用户选择不做 clean-machine 与 RC-A → RC-B 真实验收，只在之后单独授权时发版。该决定不降低 CI fail-closed 门禁，但意味着发布后不能声称 `Smoke passed` / `Release ready`，必须把首版 updater 未经真实跨版本验证列为接受风险。
- 2026-08-24：macOS-only workflow/verifier 收窄完成本地验证：14/14 release/packaging 定向合同，完整 `npm run test` 为 1222 suites / 5305 tests（5304 pass / 0 fail / 1 skip），Next production 生成 136 routes，Electron bundle 通过。正式 Developer ID/notary 证据只能由未来获准触发的 GitHub CI 产生；本地通过不替代该证据。
- 2026-08-24：macOS-only 发布收窄提交为 `8dac2864`（`ci: publish macOS updates only`）。pre-commit 再次通过 docs drift、ESLint、typecheck、harness boundary 与完整 1222 suites / 5305 tests（5304 pass / 0 fail / 1 skip）；仍无 push/tag/Release 或 workflow run。
- 2026-08-24：终审接受 P2“非 Mac 存量用户收到无可兑现资产的更新提示”。API 以资产选择结果返回 `platformAssetMissing`，不再把 Release 页伪装成 `downloadUrl`；三个 UI 消费面用 en/zh 明示无兼容安装包。同步收口六条 P3：stable/preview Mac-only 禁入表对称、metadata 恰好一条 universal ZIP、发布写权限仅授予发布 job、完整资产图先写可恢复 draft 再公开，并如实写明 Windows 手工 job 的 signer 前置。下载期检查按钮补显式反馈；>15 秒极慢 handoff 闪烁因无真实复现继续由 #89 跟踪，不用延长 timeout 冒充修复。本地 38/38 定向、完整 5308/0/1、Next production 136 routes 与 Electron bundle 通过；正式 CI 与跨版本 smoke 未跑。
- 2026-08-24：终审收口提交为 `f00d62c2`（`fix: close macOS release review findings`）。pre-commit 再次通过 lint-staged、docs drift、ESLint、typecheck、harness boundary 与完整 1222 suites / 5309 tests（5308 pass / 0 fail / 1 skip）；仍未 push、打 tag、创建 Release 或触发正式凭据 CI。
- 2026-08-24：用户明确指示“发版”，授权 push `main` 与新建不可变 stable tag。远端最新为 `v0.67.1`，`v0.67.2` 无同名 tag/Release/进行中 run，因此选择下一 patch `0.67.2`，不移动或复用旧 tag。`4d1a7e7e`（`chore(release): prepare v0.67.2`）更新 macOS-only Release Notes 与 package/lock 版本；准确发版工作树完整 5308/0/1、Next 136 routes、Electron bundle 通过。正式 Developer ID/notary/asset graph 由即将触发的 tag CI fail closed 收证；用户接受跳过的 clean-machine/RC-A→RC-B 不得在发布后补写为通过。
